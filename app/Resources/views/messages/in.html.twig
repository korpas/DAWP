{% extends '::base.html.twig' %}

{% block headTitle %}{{ productname }}{% endblock %}

{% block body %}
    {% for inn in messages %}
        {% include ':messages:content.html.twig' %}
    {% endfor %}
    {% if not doPagination is defined %}
        <div class="navigation">
            {{ knp_pagination_render(products) }}
        </div>
    {% endif %}
{% endblock %}

